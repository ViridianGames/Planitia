# Global parameters
name = Planitia

# 320x180, 640x360, 800x450, 960x540, 1280x720, 1600x900, 1920x1080
# Screen parameters
h_res = 1440
v_res = 810
h_renderres = 480
v_renderres = 270

bpp = 32
full_screen = 0
field_of_view = 10
fast_terrain = 0

mouse_cursor = Images/pointer.png
mouse_cursor_hotspot_x = 0
mouse_cursor_hotspot_y = 0

camera_zoom_speed = .2
camera_rotate_accelerate = 10
camera_rotate_topspeed = 4
camera_pan_accelerate = 10
camera_pan_topspeed = 15

camera_close_limit = 26
camera_far_limit = 48

ortho_zoom = .15

milliseconds_between_updates = 33

# Multiplayer (listen-server lockstep over ENet)
# Host listens on this UDP port; clients Join via Multi tab (default IP 127.0.0.1).
port = 43000
# Sim ticks (33ms) per lockstep turn barrier.
# 1 = one visual update per tick (~30Hz motion, 60Hz render). Same game speed.
turn_length = 1
